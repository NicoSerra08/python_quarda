lista=["ak", "nico", "chri", "solare"]

## dice chi è il più grande di questa lista
max=0
nomemax = ""

for nome in lista:
    if len(nome)>max:
        max = len(nome)
        nomemax = nome

print(nome)



